# ── EduVerse ML Recommendation Service ─────────────────────────────────────
# Python microservice using K-means + TF-IDF for course recommendations.
# Communicates with the Node.js backend via REST API.
#
# Env vars (from .env):
#   MONGODB_URI    — MongoDB connection string (same as backend)
#   ML_PORT        — Port for this service (default 5002)
#   MODEL_PATH     — Path to persist trained model (default ./model/)
# ───────────────────────────────────────────────────────────────────────────

MONGODB_URI=mongodb+srv://lacduong:lacduong212@cluster0.a6zyueg.mongodb.net/test
ML_PORT=5002
MODEL_PATH=./model
